// Reserved for future end-to-end shell. Currently unused; the canvas wires individual
// surfaces directly. Keeping this empty-ish file so the script tag in the HTML loads cleanly.
window.PUI_ShellPlaceholder = true;
