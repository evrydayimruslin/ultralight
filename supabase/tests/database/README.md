# Database Tests

Add pgTAP `.sql` tests here as the schema hardens.

`./scripts/supabase/validate-local.sh` will automatically run
`supabase test db` when at least one SQL test file exists in this directory.
